# Normalise to LF in the repository. The build server is Linux; without this,
# Windows CRLF endings get committed and every file looks modified on checkout.
* text=auto eol=lf

# Binary assets — never touch these.
*.webp binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.ico  binary
*.woff2 binary
*.zip  binary
